# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-09-08)

**Core value:** A machine-checked, concrete-bound security theorem for GCM in the same `AEADScheme.distAdvantage` framework as the completed Encrypt-then-MAC proof.
**Current focus:** Phase 4 — Wegman–Carter Authenticity Hop (6 plans; two rounds of Codex plan review done 2026-09-09 — REJECT then APPROVE-WITH-EDITS, all 11 findings confirmed and applied; ready for `/gsd:execute-phase 4`)

## Current Position

Phase: 04 of 10 (Wegman–Carter Authenticity Hop) — IN PROGRESS (6 plans; 4 waves: {04-01 ‖ 04-02} → {04-03, 04-04} → 04-05 → 04-06). Phase 03 (PRF Hop) COMPLETE (4/4 plans).
Plan: wave 1 COMPLETE (04-01 ‖ 04-02). Next: the Codex interface review on 04-01's landed skeleton (the wave-2 gate), then 04-03, 04-04 (wave 2), then 04-05, 04-06.
Status: 03-01 complete — all four generic ToVCVio bricks landed sorry-free: PRFScheme.simulateQ_prf{Real,Ideal}QueryImpl_mapM_inr (new ToVCVio/CryptoFoundations/PRF.lean), randomOracle.run'_mapM_randomOracle_fresh (new ToVCVio/OracleComp/QueryTracking/RandomOracle/FreshQueries.lean, TERM-LEVEL ProbComp equality as 03-04 requires), and evalDist_mapM_const_uniform (appended to ToVCVio/OracleComp/Constructions/BitVec.lean, verified composable with evalDist_blocksToBitVec_uniform through Functor.map_map). Both new files registered in ToVCVio.lean; axioms standard-only; full lake build green, no warnings on the touched files; scope audit clean against BASE 15628cf.
Status: 03-02 complete — SecureMessaging/AEAD/FromGCM/Security/PrfHop.lean landed (161 lines, sorry-free, registered in SecureMessaging.lean): prfReduction (eager PRF distinguisher, fixed query prefix 0 :: 1 :: counterChain 2 ((L + 127) / 128) verbatim, closed liftComp'd gcmTupleImpl tail), counterChain_length, gcmEncryptSpec_eq_tuple + gcmDecryptSpec_eq_tuple (the profile-to-tuple bridges 02-01 deferred), and game1_eq_game2 as an evalDist equality — ROADMAP Phase 3 criterion 3 FULLY DISCHARGED by one premise-free application of probOutput_simulateQ_greedyLazy_run'_eq at the whole tuple. Criterion 1's def surface landed (its projections are 03-03/03-04). Axioms standard-only (propext/Classical.choice/Quot.sound on game1_eq_game2); full lake build green (3123 jobs), no PrfHop warnings; scope audit clean against BASE 15628cf
Status: 03-03 complete — the REAL-side projection landed in PrfHop.lean (161 → 259 lines, sorry-free): run'_game0Impl_eq_tupleImpl (per-key id state-projection of game0's real-cipher oracles onto gcmTupleImpl at the real tuple, via the Phase-1 CipherProfile specializations composed with 03-02's profile-to-tuple bridges) and game0_eq_prfRealExp in the pinned Pr[= true | ·] form — ROADMAP Phase 3 criterion 2, REAL half discharged. Hypotheses exactly prp/L/hL/adv: NO [NeverFail prp.keygen], no extra instance (the eager-design payoff). Axioms standard-only on both; full lake build green (3124 jobs), zero warnings mentioning PrfHop; scope audit clean against BASE 50f5c3c (only PrfHop.lean, +99/−1, prfReduction body byte-identical).
Status: 03-04 complete — the IDEAL-side projection and the phase bound landed in PrfHop.lean (259 → 462 lines, sorry-free): prfIdealExp_prfReduction_eq (term-level ProbComp peel of the ideal experiment into three independent uniform draws; freshness/Nodup of the n + 2 eager query points from cipherInputs_pairwise_ne hL — hL is load-bearing, inc32 wraps mod 2^32), evalDist_keystream_bind (criterion 4: n uniform 128-bit blocks concatenated and truncated ARE one uniform ks : BitVec L, via 03-01's evalDist_mapM_const_uniform ∘ counterChain_length ∘ Functor.map_map ∘ Phase 1's evalDist_blocksToBitVec_uniform), game1_eq_prfIdealExp, and game0_game1_le_prf in the ROADMAP spelling |Pr[game1] − Pr[game0]| ≤ prfAdvantage prp.toPRFScheme (prfReduction L adv). ROADMAP Phase 3 criteria 1–5 ALL DISCHARGED. Phase gates: full lake build green (3124 jobs) with zero new warnings; sorry audit shows only the pre-existing PRP/Defs.lean (2) + PRFPRNG/Defs.lean entries; #print axioms standard-only on game0_game1_le_prf, game1_eq_game2, game1_eq_prfIdealExp, prfIdealExp_prfReduction_eq, game0_eq_prfRealExp; phase scope audit against PHASE_BASE 15628cf lists exactly the 6 phase files, source trees clean and free of untracked leftovers.
Status: 04-02 complete — Phase 4's ONLY missing generic brick landed sorry-free, append-only into ToVCVio/OracleComp/QueryTracking/LazySampling.lean (394 → 617 lines, +223/−0, pure insertion at line 394 below the existing consumeLazy block; NO existing declaration touched, so Games.lean and PrfHop.lean rebuild green unchanged). Four declarations, all `omit [spec.Fintype] [spec.Inhabited] in` like their neighbours: run_simulateQ_consumeLazy_some_eq (TERM-LEVEL cached-case collapse — the workhorse, public), evalDist_simulateQ_consumeLazy_run_some_eq (pinned S2-AUX, its 2-line marginal), evalDist_simulateQ_consumeLazy_run_sample_eq (S2⁺, the SAMPLE-PRESERVING joint τ × (output × state) commutation, pinned verbatim incl. the `none` → fresh `$ᵗ τ` completion branch that Codex F-3 requires), evalDist_simulateQ_consumeLazy_run_eq (S2, pinned byte-identical, corollary of S2⁺ by mapping away τ). Task 1's probe gate CLEARED: no fourth route, no HUMAN_RULING. h_indep is character-for-character the one probOutput_simulateQ_consumeLazy_run'_eq carries, so 04-05 reuses Phase 2's gcmTupleImplReject_indep discharge. Gates: full lake build green (3126 jobs), zero warnings mentioning LazySampling; zero sorry in the file; #print axioms standard-only on all four; scope audit over THIS PLAN'S OWN commit fbfef5a (not BASE..HEAD — 04-01 landed 72165c0/cdb0b7f on the same branch in the same wave) lists exactly one file, 223 insertions, zero deletions. Authorised sorry-ledger exception: this plan owns no WC obligation, so the count is unchanged by design (Codex F-5).
Status: 04-01 complete — the ENTIRE Phase-4 statement surface is in-tree, typed and elaborating, across three new files (721 lines): ToVCVio/CryptoFoundations/WegmanCarter.lean (389), ToVCVio/CryptoFoundations/WegmanCarterBound.lean (114), SecureMessaging/AEAD/FromGCM/Security/AuthHop.lean (218), registered in ToVCVio.lean and SecureMessaging.lean. PROVED (axioms propext/Quot.sound only): wcInstImpl_decrypt_run (WC0, the §A.1 decrypt normal form) and gcmInstImpl_eq_wcInstImpl (WC-BRIDGE, the 6-line funext/rcases/cases proof at enc := id, hash := fun H p => ghash H (gcmEncode p.1 p.2), D := SupportedAAD × BitVec L, padMsg = unpad = (· ^^^ ks)) — Games.lean UNTOUCHED (its diff over the plan's commits is empty). Also landed unsorried: wcSpec (inline spelling of aeadOneTimeCCASpec, identified by an example … := rfl on the SecureMessaging side — the layering resolution), wcInstImpl, WCLogState (challenge slot carries its AAD; entries carry a pre/post Bool tag), wcLogImpl (its decrypt oracle reads neither H nor mask), wcAccepts/wcFlag/wcProj. Sorry ledger, reconciled BY NAME: 13 named obligations / 14 declarations — WC1 :239, WC2 :254, WC3(1/2) support_state_measure_le_of_isQueryBoundP :274, WC3(2/2) wcLogImpl_log_length_le :294, WC3b :313, WC7 :340, WC8 :373 (WegmanCarter.lean); WC4 :68, WC5 :87, WC6 :105 (WegmanCarterBound.lean); WC9 :119, WC-IUB0 :155, WC-IUB :185, WC10 :209 (AuthHop.lean). WC-IUB0 landed UNSPLIT, so 14 declarations not 16. Gates: full lake build green (3127 jobs) with EXACTLY 14 `declaration uses 'sorry'` warnings on the three new files and no other warning of any kind; sorry diff against the pre-touch baseline (12 terms in PRP/Defs.lean + PRFPRNG/Defs.lean) is purely additive and exactly this plan's 14 bodies; scope audit over THIS PLAN'S OWN commits 72165c0/cdb0b7f/c306026 (excluding 04-02's fbfef5a on the same branch) is exactly the five files_modified.
Last activity: 2026-09-09 — Executed 04-01 and 04-02 (wave 1). 04-01's statement-discipline tripwire STAYED SILENT: every pinned statement landed with its pinned content, including the restored henc_inj and the added hfloor on WC7/WC8, WC7's (K × BitVec 128) × WCLogState A Cb carrier, and WC10's |Pr[game3] − Pr[game2]| orientation. Three elaboration-level spellings had to be adjusted (none touches statement content): the QueryImpl summand ascriptions must spell the target StateT monad rather than the pin's `_` (otherwise `get`'s state type is a metavariable and the following `match challenge with` has an ill-typed pattern variable — the 04-RESEARCH §C.3 probe reported green with `_`, but it is not green in the landed file); WC7's `fun z => ((H, mask), z.2)` needs the binder type annotation; and `μ.support` must be spelled `support μ` in ToVCVio (ProbComp reduces to unifSpec.toPFunctor.FreeM, so dot-notation resolves against PFunctor.FreeM). That the pinned 6-line bridge proof still closes verbatim is the evidence the handler term is unchanged. Previously: executed 04-02 (wave 1). Statement-discipline tripwire silent: both pins verbatim, SHAPE LATITUDE not invoked, allowed_sorries: none met. The proof came in cheaper than planned because the cached case is a TERM-LEVEL ProbComp identity. Previously: executed 03-04 (wave 3), closing Phase 3. Statement-discipline tripwire silent (all three pins verbatim, incl. the |Pr[game1] − Pr[game0]| argument order). Previously: executed 03-03 (wave 2). Statement-discipline tripwire silent; both proofs elaborated first try in /tmp probes and landed unchanged. Two planned contingencies went UNUSED: no alpha-pinning was needed on prfReduction's singleton fetches (they are top-level binds with explicit OracleQuery … (BitVec 128) ascriptions, so VCVio's simulateQ_prfRealQueryImpl_inr fires under simp only), and no show/rfl bridge was needed between toPRFScheme.eval and toBlockCipher.perm.

Progress: [████░░░░░░] 40% (phase 1 complete: 9/9; phase 07a complete: 5/5; phase 02 complete: 4/4; phase 03 complete: 4/4; phase 04 in progress: 04-02 done)

## Performance Metrics

**Velocity:**
- Total plans completed: 1
- Average duration: 4 min
- Total execution time: 4 min

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01 | 1 | 4 min | 4 min |

**Recent Trend:**
- Last 5 plans: 01-01 (4 min)
- Trend: -

*Updated after each plan completion*
| Phase 01 P05 | 6 min | 2 tasks | 1 files |
| Phase 01-structural-foundations P06 | 6 min | 2 tasks | 1 files |
| Phase 01 P02 | 6 min | 3 tasks | 1 files |
| Phase 01-structural-foundations P04 | 9 min | 2 tasks | 1 files |
| Phase 01 P03 | 9 min | 3 tasks | 1 files |
| Phase 01-structural-foundations P07 | 3 min | 2 tasks | 1 files |
| Phase 01 P08 | 5 min | 2 tasks | 1 files |
| Phase 01-structural-foundations P09 | 3 min | 2 tasks | 1 files |
| Phase 07a P01 | 4 min | 2 tasks | 2 files |
| Phase 07a P02 | 10 min | 2 tasks | 2 files |
| Phase 07a P03 | 19 min | 2 tasks | 1 files |
| Phase 07a P04 | 3 min | 2 tasks | 1 files |
| Phase 07a P05 | 4 min | 2 tasks | 1 files |
| Phase 07a P05 | 4 min | 2 tasks | 1 files |
| Phase 02 P01 | 8 min | 2 tasks | 2 files |
| Phase 02 P02 | 4 min | 2 tasks | 1 files |
| Phase 02 P03 | 6 min | 2 tasks | 1 files |
| Phase 02 P04 | 9 min | 2 tasks | 1 files |
| Phase 03 P02 | 5 min | 2 tasks | 2 files |
| Phase 03-prf-hop P01 | 15 min | 3 tasks | 4 files |
| Phase 03 P03 | 4 min | 2 tasks | 1 files |
| Phase 03 P04 | 6min | 3 tasks | 1 files |
| Phase 04 P02 | 15 min | 2 tasks | 1 files |
| Phase 04 P01 | 24 min | 3 tasks | 5 files |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Phase 1 (01-01, executed): `keystream` de-privatized in GCM.lean per research Open Question 1 — privacy would force keystream-unfolding lemmas into the NIST spec file, violating REQ-07 placement
- Phase 1: AXU predicate stated via `probOutput` over `ProbComp` (matches Phase 4/7b consumers)
- Phase 1: generic AXU predicate + floor in `ToVCVio/CryptoFoundations/UniversalHash.lean`; GCM instantiation in `SecureMessaging/AEAD/FromGCM/Security/Axu.lean`
- [Phase 01]: 01-08: unequal-lenA distinctness proven entirely at reversed position 0 (length block) per roadmap lock — no leading-coefficient or list-length comparison; sigma trap avoided by substituting the lenA equality before touching payloads; gcmEncode_injective derived as contrapositive of gcmEncode_tail_distinct so the equal-lenA append_inj argument lives in one place
- [Phase 01]: 01-07: single-sample OTP form kept as GCM-named entry point with m on the XOR left (Phase 5's argument order, no xor_comm detour at use sites); block form by 3-step calc (Functor.map_map, probOutput_xor_map, 01-04 brick) so the proof stays visibly counting-free; keystream bridge closed by rfl as 01-04 engineered
- [Phase 01]: 01-05: lenBlock_ne_of_ne stated at general bound a,b < 2^64 so 01-08 applies it without re-deriving the AAD bound; gcmEncrypt connection lemma is a full rfl unfolding for one-step game rewriting
- [Phase 01]: 01-02: profile theorems packaged via explicit spec defs (gcmEncryptSpec/gcmDecryptSpec) with h/mask/ksBlocks as leading explicit parameters — Phase 2 games substitute sampled values positionally; specializations stated with prp.toBlockCipher.perm (Construction.lean's exact term)
- [Phase 01]: 01-06: AXU predicate uses [XorOp T] (Lean v4.32 core renamed the ^^^ typeclass; Xor is now Prop-level); floor lemma card_inv_le needs no Nonempty T (card T != 0 by contradiction from 1 <= card T * eps); sums-to-one via sum_probOutput_eq_one, no tsum sublemma needed
- [Phase 01]: 01-04: uniformity via split bijection (blocksSplit) chained through evalDist_map_bijective_uniform_cross + evalDist_map_fst_uniformSample_prod, not fiber counting; block width kept at 128 so blocksToBitVec matches GCM.keystream verbatim for 01-07's rfl bridge; evalDist-level theorem provided alongside the probOutput shape
- [Phase 01]: 01-03: counterChain_ofNat non-wrap hypothesis stated as c + m <= 2^32 (plan's 2^32 - 1 bound cannot reach the ValidMsgLength boundary n = 2^32 - 2); inc32 non-wrap packaged at toNat level (x.toNat % 2^32 < 2^32 - 1); distinctness delivered as Pairwise (ne) on the concrete list 0 :: 1 :: counterChain 2 n
- [Phase 01]: 01-09: floor shipped in both normal forms (card-form direct from generic lemma, 2^128-form via FinEnum.card_eq_fintypeCard bridge) so Phase 4 consumes either without conversion; witness distinctness via congrArg on a.1.1 (0 vs 8), no Subtype.ext_iff/HEq; GhashIsAXU kept a def with a rfl example pinning the unfolding in CI
- [Phase 07a]: 01: nistPoly grouped as X^128 + (X^7+X^2+X+1) so monic_X_pow_add applies with the tail as p; monic + natDegree=128 both via compute_degree!; root annihilation stated with modulus written literally as nistPoly (only the reduced element spelled out) so mk_X yields root nistPoly and avoids an atom mismatch under ring; char 2 taken as (2 : AdjoinRoot nistPoly)=0 via map_ofNat on AdjoinRoot.of (no CharP/CharTwo instance exists for AdjoinRoot), reduction closed by linear_combination; set_option maxRecDepth 4000 needed for ring over root^128, and it must precede the docstring
- [Phase 07a]: 07a-02: BitVec half is a plain Equiv + separate reflect_xor (Mathlib BitVec + is arithmetic not XOR); cross PowerBasis.dim=natDegree defeq with change not rw; Basis renamed to Module.Basis
- [Phase 07a]: 07a-05: reversed-block indexing locked (coefficient of h^(i+1) is blocks.reverse[i], verbatim match to Encoding.lean gcmEncode_tail_distinct — Phase 7b transport index-for-index); horner_foldl_eq_sum by List.reverseRecOn (append-singleton multiplies acc by H and prepends b at reverse index 0), reindex via one Finset.sum_range_succ' + sum_mul + mul_assoc + pow_succ; reflect_ghash_foldl_gen seed-generalized (foldl accumulator varies; seed 0 via reflectN_zero); ghashPoly packaging uses renamed eval_finsetSum/finsetSum_coeff (old eval_finset_sum/finset_sum_coeff deprecated 2026-04-08); ghashPoly_natDegree_le ≤ blocks.length proven (natDegree_C_mul_le∘natDegree_X_pow_le∘omega) as the maxBlocks bound for 7b
- [Phase 07a]: 07a-04: reflect_gfmul_aux dropped the plan's k ≤ 128 hypothesis (invariant is unconditional since reflect_gfmulStep is bit-index agnostic — unused binder avoided); stated as a DUAL conjunction so the z-step's ⊕v term can read the v-step's reflectN y·root^k; gfmulStep's 2nd component IS vStep p.2 so gfmul_eq_foldl is rfl and the succ .2 is defeq (change, not rw); reflectN_zero via reflectN_apply + Finset.sum_eq_zero (not self-cancellation — 0^^^0's 0#128 misses BitVec.xor_zero's 0#w); base case by `change` onto (0 : BitVec 128) (simp normalizes to 0#128 which reflectN_zero won't rewrite); reflect_gfmul = invariant at k=128 with coefficient-sum = reflectN x via Fin.sum_univ_eq_sum_range
- [Phase 07a]: 07a-03: reflectN defined as an Equiv structure literal using BitVec.cast on both legs (not `▸`/_root_.cast) so ⇑reflectN unfolds by defeq and getMsbD_cast/xor_cast reduce; the natDegree=128 transport is discharged once in reflectN_apply (Fintype.sum_equiv (finCongr nistPoly_natDegree)) and never fought again; reflect_gcmReductionConst collapses the 128-term sum to range 8 (generic vanishing above) + 8 single-index decides (no full-128 decide); reflect_gfmulStep = two-case boolean split on getMsbD 127 (= getLsbD 0), the ⊕R branch supplies root^128 that duplicates the shift's escaped term and cancels via 2•root^128=0; heavy coordinate lemmas (reflectN_mul_root_left/reflectN_ushiftRight_eq) pulled top-level for heartbeat budget — congr 1 on the AdjoinRoot sum equation times out, replaced by simp only [smul_mul_assoc, ← pow_succ] + one Fin.sum_univ_castSucc
- [Phase 02]: 02-02: hit predicate spelled (fun t => t matches OEncrypt _) with NO decide wrapper (`matches` elaborates to Bool directly) — identical term in gcmTupleImplReject_indep/game3/game4 so Phases 3/4/5 pass the lemma with zero predicate conversion (direct passability into probOutput_simulateQ_consumeLazy_run'_eq verified by throwaway elaboration); gcmTupleImplReject_indep non-hit branches closed by plain rfl (statement-discipline tripwire silent — 02-01 factoring sound); gcmRandRejectImpl ignores its tuple binder (_a + nolint) so its own h_indep is immediate and the dead hit-sample falls to uniform losslessness in 02-04's game4_eq_rand
- [Phase 02]: 02-01: gcmGameSkeleton returns a QueryImpl (unifImpl + encImpl + decImpl) over StateT (Option C) (OracleComp spec) — simulateQ/run' lives in the game defs so LazySampling's implFam interface composes; tuple locked as (H, mask, ks) with ks : BitVec L (flat keystream, profile-to-tuple bridge deferred to Phase 3); game0 uses gcmOneTimeAEAD.encrypt/decrypt directly at a key sampled outside the skeleton with plain Option C state (id-projection for game0_eq_real); game1 pins K/L via underscore-prefixed _prp/_hL + nolint (EtM encReduction pattern); DecidableEq SupportedAAD deliberately not pinned (ciphertext-only guard)
- [Phase 02]: 02-03: gcmInstImpl decrypt acceptance spelled with explicit decide (ok : Bool); simp's decide_eq_true_eq reconciles it against gcmTupleImpl's propositional if inside the flag-erasure proof — no bridging lemma; flag-erasure exposed as two named helper lemmas quantified over an arbitrary initial flag (Phase 4 can invoke at any flag state); composite projections proved by bind_congr + rw since flag erasure holds as a ProbComp equality (stronger than evalDist), then closed exactly by the lazy commutation lemmas
- [Phase 02]: 02-04: game4_eq_rand's h_indep is the inline fun _ _ _ _ _ => rfl (gcmRandRejectImpl ignores its τ-argument at EVERY query — the two runs are the same term); uniformSample_prod_eq_bind NOT needed in the encrypt hproj branch (both sides draw the joint $ᵗ (BitVec L × BitVec 128), unlike EtM RandHop's componentwise sample); [NeverFail prp.keygen] kept on BOTH endpoints per criterion 6 (redundant on the current VCVio checkout via the blanket total-PMF-lift instance, kept for contract stability); dead-tuple representative after unconsume is gcmRandRejectImpl default with r supplied explicitly to probOutput_bind_of_const and per-a constancy by rfl
- [Phase 03]: 03-02: prfReduction's do-block needs an outer ascription to OracleComp (PRFOracleSpec (BitVec 128) (BitVec 128)) Bool — PRFAdversary D R is a 2-arg abbrev whose SECOND arg is the oracle range (return type is always Bool), so do-notation HOU-unifies the monad with PRFAdversary (BitVec 128) and fails Monad/MonadLiftT synthesis; every pinned body property preserved verbatim (no coercions/List.range/local n). counterChain_length placed in PrfHop.lean not Counter.lean (Phase-1 files untouched). VCVio's four singleton PRF forwarding lemmas fire under simp only, NOT plain simp (full simp first applies simulateQ_spec_query, after which the forwarding lemma no longer matches and is flagged unused) — actionable for 03-03/03-04. gcmDecryptSpec_eq_tuple needs a trailing rfl (ite Decidable-instance mismatch; both sides print identically); gcmEncryptSpec_eq_tuple closes with simp only alone. game1_eq_game2 closed exactly as predicted: unfold game1 game2 + exact probOutput_simulateQ_greedyLazy_run'_eq gcmTupleImpl adv none (premise-free, whole tuple).
- [Phase 03]: 03-01: the two PRF mapM forwarding lemmas cannot use VCVio's singleton _inr lemmas directly — inside a List.mapM body the lifted query's result type is forced to R while the VCVio lemmas carry Range (Sum.inr ?d), and neither rw nor simp can solve Range (Sum.inr ?d) =?= R with ?d unassigned; fixed by two private alpha-pinned restatements proved by term-mode application (defeq), the same device as EtM PrfHop's hroI (expect the same gotcha on the singleton H/mask fetches in 03-03/03-04)
- [Phase 03]: 03-01: FreshQueries.lean uses namespace randomOracle (VCVio RandomOracle/Basic.lean convention), so consumers write randomOracle.run'_mapM_randomOracle_fresh; the brick uses the global @[simp] StateT.run'_bind' safely (its continuation is a lifted plain ProbComp — no folded simulateQ .run' to protect, so EtM's hpush pattern was unnecessary), then withCaching_run_none + bind_map_left per element, tail freshness from Nodup + cacheQuery_of_ne, uniformSampleImpl t = $ᵗ R by rfl
- [Phase 03]: 03-01: evalDist_mapM_const_uniform by the counting route (pointwise probOutput on both sides, glued by evalDist_ext; wrong-length branch via probEvent_eq_zero), both pointwise computations as private top-level lemmas; DecidableEq R via letI := Classical.decEq R (the classical tactic left Decidable (r = y) to a synthInstance heartbeat timeout); [Fintype R] is proof-only so the pinned statement trips linter.unusedFintypeInType — suppressed with a scoped set_option rather than weakening the statement
- [Phase 03]: 03-03: NO alpha-pinning needed on prfReduction's singleton H/mask fetches — they are top-level binds carrying explicit OracleQuery … (BitVec 128) ascriptions, so VCVio's simulateQ_prfRealQueryImpl_inr fires directly under simp only; 03-01's Range (Sum.inr ?d) =?= R wall is confined to List.mapM interiors, which its own _mapM_inr brick absorbs. Also no show/rfl bridge between toPRFScheme.eval and toBlockCipher.perm: toPRFScheme is a structure literal (eval := prp.perm) and PRPScheme extends BlockCipher, so simp only lands on exactly the spelling gcmOneTimeAEAD_encrypt_profile produces.
- [Phase 03]: 03-04: the alpha-pinning wall DOES reach prfReduction's singleton fetches — but only at the PEEL step, not the simp only collapse: after collapsing, the leading bind's bound-variable type is the reducible (PRFOracleSpec _ _).Range (Sum.inr _), so `rw [run'_randomOracle_bind_of_none 0 …]` fails on Range (Sum.inr ?d) =?= BitVec 128. Repair is cheaper than 03-01's: apply the peel in TERM mode, `refine Eq.trans (peel …) ?_` (term elaboration unifies at default transparency), no alpha-pinned restatement needed
- [Phase 03]: 03-04: the peel factored as a generic private lemma run'_randomOracle_bind_of_none (one cache miss = one top-level $ᵗ draw) using a targeted StateT.run'_eq + StateT.run_bind push, never the global @[simp] StateT.run'_bind' (which would dismantle the folded (simulateQ (gcmTupleImpl …) adv).run' none in the tail); uniformSampleImpl t = $ᵗ R must be named as a local `have … := rfl` for simp to close the last step
- [Phase 03]: 03-04: VCVio's evalDist_bind_congr_left is the RIGHT-factor congruence (varies the continuation); the keystream reshape needs the LEFT-factor one (𝒟[x] = 𝒟[y] → 𝒟[x >>= f] = 𝒟[y >>= f]), hand-rolled as a private two-liner from evalDist_bind. The ideal side must work at evalDist level (blocksToBitVec is non-injective when L < 128⌈L/128⌉) and converts once via ToVCVio.probOutput_eq_of_evalDist_eq — unlike the real side, which is a term equality throughout
- [Phase 03]: 03-03: the ODecrypt branch of run'_game0Impl_eq_tupleImpl needs no by_cases guard split (unlike Games.lean's flag-erasure lemmas) — both impls are gcmGameSkeleton instances sharing the ACD19 ciphertext-only gate syntactically, so only the decryptResp bodies differ and one simp with profile + bridge closes it. game0_eq_prfRealExp needs no NeverFail: (prp.toPRFScheme).keygen = prp.keygen is rfl (research Pitfall 8 confirmed).
- [Phase 04]: 04-02: the consumeLazy cached case is a TERM-LEVEL ProbComp identity (run_simulateQ_consumeLazy_some_eq) — consumeLazy never overwrites a populated cache in either branch, so from (s, some a) it IS implFam a with some a threaded along; this makes S2+'s hit branch a term equality via congrArg evalDist (no support argument needed to know the final cache is some a) and yields the pinned evalDist S2-AUX as a 2-line corollary
- [Phase 04]: 04-02 probe verdict: NO fourth route to S2. (a) reshaping oa to carry the state is structurally impossible (sigma lives in the simulator, not in spec) and even the spec-enlargement repair cannot deliver S2+ (a sigma-reading oracle still cannot report the cached sample); (b) the only run/run' bridge in VCVio is StateT.run'_eq, the lossy direction — run' determines only the marginal; (c) DeferredSampling.evalDist_bind_comm is not a shortcut, it IS the direct route's non-hit step, already available in-file as probOutput_bind_bind_swap. Near miss for the record: simulateQ_run_eq_of_snd_invariant (StateProjection.lean:295) does cover the cached case at q0 = some a, but needs a fixSndStateT funext dance longer than the 15-line direct induction and gives only the weaker evalDist form
- [Phase 04]: 04-02: S2 proved by MAPPING S2+ (congrArg (Prod.snd <$> .) then ← evalDist_map), not by re-induction; the completion draw's losslessness is discharged pointwise with no new import — probOutput_bind_const is @[simp] and Pr[⊥ | $ᵗ τ] = 0 falls to simp, so rcases on z.2.2 + simp [Prod.map] closes both branches (DeferredSampling.evalDist_bind_const_neverFails NOT imported). Gotcha: probOutput_bind_const cannot be rw'd before the shapes are fixed (EvalDistCompatible ?m gets stuck) — convert the RHS map with map_eq_bind_pure_comp and reduce with probOutput_bind_congr' first
- [Phase 04]: 04-01: wcInstImpl/wcLogImpl QueryImpl summand ascriptions must spell the target monad (StateT ... ProbComp) — the pinned '_' leaves the state type a metavariable when 'let (challenge, forged) <- get' elaborates, so the following 'match challenge with' has an ill-typed pattern variable; Games.lean's gcmInstImpl already spells it out and the pinned 6-line bridge proof closes unchanged, confirming the term is identical
- [Phase 04]: 04-01: 'mu.support' must be written 'support mu' in ToVCVio statements — ProbComp reduces to unifSpec.toPFunctor.FreeM, so generalized field notation resolves .support against PFunctor.FreeM and errors
- [Phase 04]: 04-01: WC-IUB0 gcmInstImpl_identicalUntilBad landed UNSPLIT (one three-way conjunction) — split latitude unused, so the phase ledger is 13 named obligations / 14 sorry declarations, and 04-06 discharges 4 declarations not 6
- [Phase 04]: 04-01: section-variable layout for the sorry skeleton — only the five pins without their own type binders (WC0, WC1, WC2, wcLogImpl_log_length_le, WC3b) sit under 'variable {K A M Cb D : Type}'; alpha is never a section variable, so no pinned binder shadows one (autoImplicit = false)

### Pending Todos

None. (Crypto-eval todo completed 2026-09-09: .claude/agents/crypto-eval.md created; cross-engine eval of Phase 2 ran via codex exec --sandbox read-only, gpt-6-astra, high reasoning; 02-EVAL.md persisted — DECISION: ACCEPT, 20/20 refutations survived, zero findings, no sorry/axiom in diff. Phase 2 fully closed.)

Phase 3 crypto-eval also complete 2026-09-09, same cross-engine dispatch (codex exec --sandbox read-only, openai/gpt-6-astra, reasoning=high, ~114k tokens): 03-EVAL.md persisted — DECISION: ACCEPT, 21/21 refutations survived, zero findings, none in diff on the sorry/axiom audit. Dispatch gotcha: ~/.secrets/openrouter holds an `export …` line, so it must be SOURCED (`set -a; source ~/.secrets/openrouter; set +a`) — `cat`-ing it into OPENROUTER_API_KEY yields 401 Missing Authentication header. Two eval results worth carrying: (a) the `n + 2` count is a FUNCTION-oracle query count only — uniform-oracle traffic in the reduction's tail is additional, so Phase 9's IsQueryBoundP statement must be about the function summand of PRFOracleSpec, not all queries; (b) `hL`'s upper-bound conjunct alone is what buys chain freshness (byte alignment is not needed) — at the excluded L = 2^39 − 248 the chain's last point would wrap to 0 and return the cached H, and the maximum admitted L = 2^39 − 256 stops exactly one point short.

### Blockers

None.

## Session Continuity

Last session: 2026-09-09
Stopped at: **Phase 4 plan review TRIAGED — plans revised, re-review owed.** The
`/crypto-plan-review 4` run (codex exec, openai/gpt-6-astra, high, read-only, ~14 min,
P01–P46) was cut off by a power failure while emitting its final message ("stream
disconnected before completion"); it was recovered by RESUMING the same codex session
(`codex exec --sandbox read-only … resume 01a0859d-bc0b-7922-9515-b84c1b5295e3 "output the
final message"`) — no re-probe, ~4 min. Note the CLI ordering gotcha: global flags must
precede the `resume` subcommand. Artifact: `04-CODEX-REVIEW.md` (verbatim review +
provenance header + planning-seat triage).

VERDICT **REJECT**: 1 blocker + 5 major + 1 minor, ALL SEVEN independently re-verified and
CONFIRMED, all fixed in the plan files. The blocker is a real soundness bug of the same
species as EtM's "false brick 2": WC7/WC8 as pinned were FALSE, because
`IsAlmostXorUniversal` quantifies only over pairs `x ≠ y` (UniversalHash.lean:34), so on a
subsingleton domain it is vacuous and `ε = 0` is admissible while a blind pre-challenge tag
guess still wins with `2⁻¹²⁸`; both escapes the plan had authorised are refuted (no encrypt
query ⇒ no post entry; and a post entry shares the challenge's DOMAIN point under
`henc_inj`). Fix: `hfloor : 2⁻¹²⁸ ≤ ε` on WC7/WC8, discharged free at GCM by
`ghashAXU_eps_lower`. Other fixes: WC7's carrier is `(K × BitVec 128) × WCLogState A Cb`
(F-2); 04-02's deliverable becomes the SAMPLE-preserving S2⁺ with the marginal S2 as
corollary, since the bad event reads the mask (F-3); new typed obligation **WC-IUB**
`gcmInst_tvDist_le_probEvent_forge` staged in 04-01 for ROADMAP criterion 1's
identical-until-bad staging, closed by 04-06 — ledger now 12 obligations / 13 sorry bodies
(F-4); bounded `declaration uses 'sorry'` warning exception + body-level sorry counting
(F-5); ownership-scoped per-plan scope audits instead of shared `BASE..HEAD` (F-6);
two-clause WC3b induction invariant (F-7). Nothing structural changed — the five-step chain,
plan split and waves survive. Round 2 then ran on the revised set (`04-CODEX-REVIEW-2.md`): **APPROVE-WITH-EDITS**, 4
findings, all confirmed and applied. It certified R1-F-1/2/3/6/7 genuinely closed, R1-F-4
"papered over" and R1-F-5 "partially fixed". The two substantive round-2 edits: (a) the
`wcFlag`/`wcProj` pins had UNTYPED binders `(hash) (enc) (H) (mask)` — fatal under this
project's `autoImplicit = false` / `relaxedAutoImplicit = false` (lakefile.toml:12-13), since
the holes' `?K`/`?D` never meet the ambient `K`/`D`; (b) WC-IUB staged only the output-DISTANCE
inequality, which is strictly weaker than criterion 1's identical-until-bad STATE relation
(two handlers can agree on responses and bad flags while writing different hidden base
states), so a new obligation **WC-IUB0** `gcmInstImpl_identicalUntilBad` bundling the three
per-oracle conditions is staged in 04-01 and closed by 04-06 ahead of WC-IUB. Also: sorry
accounting now snapshots the REAL pre-existing baseline (12 terms — PRP 2, PRFPRNG 10, three
of them INLINE at :163/:164/:171 and invisible to an anchored `^\s*sorry\s*$` search) and
diffs against it, warning allowances count sorried DECLARATIONS not ledger entries (14 at
04-01, WC3 being one entry with two declarations), and 04-06's final gate no longer demands
raw `grep "sorry"` return only two files (it would fail on the module documentation 04-01 is
required to write). Ledger: **13 obligations / 14 bodies**, draw-down 13 → 9 → 6 → 4 → 0.
One round-2 finding was a correction to MY round-1 triage: a post entry is NOT forced to
share the challenge's hash-domain point (at `enc ((), c) := c` with distinct bodies they
differ) — it merely MAY, so it does not guarantee the distinct-domain witness `card_inv_le`
needs. The `hfloor` fix does not depend on the difference. Two post-review loose ends were then
closed on the user's go-ahead: WC-IUB0's spec-indexed binders are spelled out in full rather
than left as `(t : _)` / `(u : _)` (Domain = index type, Range spec t = spec t, VCVio
OracleSpec.lean:43-44; spec₂ := unifSpec since ProbComp IS OracleComp unifSpec; unqualified
`aeadOneTimeCCASpec` comes from `open AEADScheme`, Games.lean:39), and WC-IUB0 carries an
explicit split latitude (three declarations under one ledger entry, the WC3 precedent) so an
awkward conjunction does not become an escalation. Counts: 13 obligations / 14 declarations
as pinned, 16 if split — every gate reconciles BY NAME, not by count.
NEXT: `/gsd:execute-phase 4`.

Previously stopped at: **Phase 4 PLANNED** — 6 plans, 4 waves ({04-01, 04-02} → {04-03, 04-04} → 04-05 → 04-06), plan-checker VERIFICATION PASSED with zero blockers. Research (04-RESEARCH.md, 734 lines) + planning together overturned FOUR things from the discussion, all now fixed in 04-CONTEXT.md: (1) the GCM instantiation was at the WRONG AXU domain — must be `enc := id`, `hash := fun H p => ghash H (gcmEncode p.1 p.2)`, `D := SupportedAAD × BitVec L`, since AXU of raw `ghash` on block lists is FALSE (Axu.lean:16-21); (2) the "unified pre/post" decision is UNSOUND and was REVERSED — after H-hoisting a pre-challenge degenerate query is 0/1 given the run (its 2⁻¹²⁸ lives in T*, drawn INSIDE the run), so per-index charging gives ε + 2⁻¹²⁸ ≤ 2ε; the sound structure is ROADMAP criterion 3's disjoint pre/post index sets combined via REALIZED counts (𝔼[|preLog|]·2⁻¹²⁸ + 𝔼[|postLog|]·ε ≤ q_d·ε) — do NOT bound the two terms by q_d separately; (3) `EvalDistBindComm` does NOT cover step 2 (existing commutations are run'-only, the bad event reads final state) — a `.run`-level commutation is NEW generic ToVCVio work, the phase's only missing brick, and was moved to its own wave-1 plan 04-02 to surface the risk first; (4) `henc_inj` was wrongly dropped then RE-CORRECTED — it is load-bearing for the GENERIC theorem (at non-injective `enc`, a query with `enc (ad',C') = enc (ad*,C*)`, `C' ≠ C*`, `T' = T*` clears the guard and accepts with probability 1), restored on the two bound lemmas and discharged by `Function.injective_id`. Also: ToVCVio CANNOT import SecureMessaging (AEAD/Defs.lean:12 imports ToVCVio), so the brick spells `wcSpec` inline and pins `= aeadOneTimeCCASpec …` by `rfl` on the SecureMessaging side. Bridging lemma to `gcmInstImpl a false` is PROBE-VERIFIED (9 lines; plain `rfl` fails on a matcher constant, `funext t; rcases t; funext s; cases ch <;> rfl` closes it). Sorry ledger: 11 NAMED obligations WC1–WC10 landed by 04-01, 11 → −4 (04-03) → −3 (04-04) → −2 (04-05) → −2 (04-06) → 0; per-plan `allowed_sorries` names each obligation; 04-02 and 04-06 are `none`. This inverts the usual per-plan sorry-free discipline FOR THIS PHASE ONLY (branch is local-only, CI unaffected). Gates: Codex interface review between wave 1 and wave 2 (checks the AXU-domain instantiation + pre/post log shape), post-execution cross-engine crypto-eval after 04-06. Two probe gates with stop-and-escalate → HUMAN_RULING: 04-02 Task 1 (fourth route to S2) and 04-04 Task 1 (the preLog locus argument — the phase's largest remaining unknown, verified mathematically but not mechanically). Next: `/gsd:execute-phase 4`.

Previously stopped at: Phase 4 context gathered — `.planning/phases/04-wegman-carter-authenticity-hop/04-CONTEXT.md`. Four areas discussed and locked: (1) brick genericity — new `ToVCVio/CryptoFoundations/WegmanCarter.lean`, tag pinned to `BitVec 128` with `K`/`D`/`A`/`M`/`C` abstract, concrete handler over `aeadOneTimeCCASpec A M C` + one bridging lemma to `gcmInstImpl a false` (Games.lean stays FROZEN), `X*` independence structural (deliberate deviation from criterion 1's typed-sorry obligation); (2) reparameterization — a locked FIVE-STEP chain (log-refine → sink mask → local bijection at the encrypt query → hoist H to the end → pure bound), since criterion 3(b)'s literal top-level reading is unreachable (`X*` not available before the run), with unified pre/post AXU treatment and two cheap residual sub-cases (post-challenge `X'ᵢ = X*` contributes 0 via the guard; pre-challenge contributes 2⁻¹²⁸); (3) union bound — raw guard-passing query log, `log.length ≤ q_d` as a SEPARATE support-level lemma via `IsQueryBoundP.simulateQ_run_of_step` (decoupling the budget induction from the probability argument, unlike EtM), `probEvent_exists_finset_le_sum` reused, bound stated in ℝ with `ε.toReal` + `hε : ε ≠ ⊤`; (4) delivery — in-tree sorry skeleton as 04-01 with a phase-only gate change (sorry count strictly decreases per plan, zero at phase end), FIVE plans in waves 01 → {02, 04} → 03 → 05, standard gates PLUS a Codex review of the landed skeleton after 04-01, fallback = explicit pre/post split then HUMAN_RULING. Key structural finding driving all of this: `gcmInstImpl a false`'s decrypt oracle is `pure none` in EVERY branch, so criterion 1's "hidden results don't influence visible behavior" is structural and the flag is write-only. Next: `/gsd:plan-phase 4`.

Previously stopped at: Completed 03-04-PLAN.md — **Phase 3 COMPLETE** (all 4 plans; PrfHop.lean at 462 lines, sorry-free, axioms standard-only). Landed this plan: prfIdealExp_prfReduction_eq, evalDist_keystream_bind, game1_eq_prfIdealExp, game0_game1_le_prf (commits cbf3405, 0e32faa, 1e1c3e0; BASE 1f71510, PHASE_BASE 15628cf). Phase gates all green (full build, sorry audit, #print axioms, scope audit). Next: /gsd:plan-phase 4 (Wegman–Carter authenticity hop). HAND-OFF: (a) Phase 6's triangle inequality consumes game0_game1_le_prf in exactly the |Pr[game1] − Pr[game0]| orientation; (b) Phase 9's PRP/PRF switching bound should be stated against this same prfReduction L adv, whose query count is the adversary-independent ⌈L/128⌉ + 2 (counterChain_length); (c) two generic proof devices are currently private in PrfHop.lean and worth promoting to ToVCVio at a second consumer — run'_randomOracle_bind_of_none (one-step cache-miss peel, generic in (D →ₒ R)) and evalDist_bind_left (left-factor evalDist congruence, absent from VCVio).

Previously stopped at: Completed 03-03-PLAN.md — Phase 3 wave 2 COMPLETE (game0_eq_prfRealExp + run'_game0Impl_eq_tupleImpl in PrfHop.lean; ROADMAP criterion 2 real half discharged; BASE 50f5c3c, commits 7eb7e33 + 1f71510). Next and last in this phase: 03-04 (ideal projection + advantage bound). HAND-OFF for 03-04: (a) `import ToVCVio.CryptoFoundations.PRF` is already in PrfHop.lean — no import edit needed; (b) expect PRFScheme.simulateQ_prfIdealQueryImpl_inr to fire under `simp only` on the singleton fetches with NO alpha-pinning (03-03 finding — the pinning wall is mapM-interior-only, absorbed by 03-01's _mapM_inr); (c) the fresh-queries brick is `randomOracle.run'_mapM_randomOracle_fresh`; (d) Codex F-2: the ideal peel helper must carry `(hL : ValidMsgLength L)` for cipherInputs_pairwise_ne; (e) assembly is `unfold PRFScheme.prfAdvantage; rw [game0_eq_prfRealExp …, game1_eq_prfIdealExp …]; exact le_of_eq (abs_sub_comm _ _)`.

Previously stopped at: Phase 3 wave 1 COMPLETE — 03-02 (PrfHop.lean: eager prfReduction, counterChain_length, both profile-to-tuple bridges, game1_eq_game2 / criterion 3) and 03-01 (four generic ToVCVio bricks: the two PRF mapM forwarding lemmas, randomOracle.run'_mapM_randomOracle_fresh, evalDist_mapM_const_uniform), each scope-audited separately against BASE 15628cf. Next: 03-03 (real projection), then 03-04 (ideal projection + bound). NOTES for 03-03/03-04: (1) VCVio's four singleton PRF forwarding lemmas fire under `simp only`/`rw`, NOT plain `simp`; (2) they also fail to fire once the lifted query's result type is pinned to R (e.g. inside a List.mapM body) — restate privately with the type pinned and prove by term-mode application, as ToVCVio/CryptoFoundations/PRF.lean does; (3) the fresh-queries brick lives in `namespace randomOracle`
Resume file: .planning/phases/04-wegman-carter-authenticity-hop/04-03-PLAN.md
